apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-server
spec:
  ingressClassName: nginx
  rules:
    - host: api.marriott.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-server
                port:
                  number: 80